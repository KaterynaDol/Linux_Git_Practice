# Homework 11 — Permission Checker

## 📋 Описание
Скрипт `permission_checker.sh` автоматически проверяет все файлы в папке `/opt/010825-ptm/kateryna` и добавляет права на исполнение (`chmod +x`) всем файлам с расширением `.sh`.

## ⚙️ Как работает
1. Находит все файлы в целевой директории.
2. Проверяет, оканчивается ли имя файла на `.sh`.
3. Если да — добавляет право на исполнение.
4. Выводит сообщение о каждом изменённом файле.

## 🧪 Пример запуска
```bash
./permission_checker.sh
